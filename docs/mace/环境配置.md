# MACE 环境配置

这条线只负责 MACE 训练、标注和 MD。训练环境与分析环境应当分开。

## 1. 训练环境

- conda 环境名：`ADL_MACE`
- 后端：`MACE`
- 训练 / MD 设备：CPU only
- 关键外部程序：`xtb`、`g16`、`Gau_Mlatom.py`

MACE 训练环境推荐直接按工程里的 CPU 方案准备：

- `environment.cpu.yml`
- `scripts/install_mace_deps_stepwise.sh`

## 2. 训练环境里至少要能通过的检查

```bash
python scripts/check_environment.py --config configs/base.yaml --strict
python scripts/check_environment.py --config configs/base.yaml --strict --test-mlatom-xtb
```

严格模式下，MACE 会要求：

- `yaml`
- `mlatom`
- `pyh5md`
- `joblib`
- `sklearn`
- `torch`
- `mace` 或 `mace_torch`
- `xtb`
- `g16`（如果 target method 使用 Gaussian）

## 3. 服务器启动建议

```bash
source /share/apps/gaussian/g16-env.sh
source /share/env/ips2018u1.env
source ~/.bashrc
conda activate ADL_MACE
export PATH=/share/apps/gaussian/g16:/share/pubbin:/share/home/Chenlehui/bin:/share/apps/xtb-6.7.1/xtb-dist/bin:$PATH
export dftd4bin=/share/apps/dftd4-3.5.0/bin/dftd4
export TORCH_FORCE_NO_WEIGHTS_ONLY_LOAD=1
```

MACE 当前固定 CPU-only，不需要 `--expect-gpu`。

## 4. 分析环境单独准备

推荐单独准备一个 notebook / 画图环境，例如：

```bash
conda create -n ADL_analysis_mace python=3.10 -y
conda activate ADL_analysis_mace
python -m pip install jupyterlab pandas matplotlib seaborn pyyaml
```

## 5. 当前 seed 约定

MACE 当前只认：

- `geometries/ture_seed/TS.log`
- `geometries/ture_seed/ts_seed.json`
- `geometries/ture_seed/ts_seed.xyz`

旧的 `geometries/seed/` 与旧命名 `geometries/ts_seed/` 都已经退役。
