# MACE 训练流程

对应工程：`minimal_adl_ethene_butadiene_mace`

## 0. 安装前置（先完成）

请先完成：

- `docs/mace/环境配置.md`

尤其是分步安装与门禁检查：

```bash
python scripts/check_environment.py --config configs/base.yaml --strict
python scripts/check_environment.py --config configs/base.yaml --strict --test-mlatom-xtb
```

## 1. 最小联通测试

```bash
conda activate ADL_MACE
cd /share/home/Chenlehui/work/test_ADL/minimal_adl_ethene_butadiene_mace
python scripts/run_first_round_pipeline.py \
  --config configs/base.yaml \
  --to-stage sample_initial_geometries \
  --submit-mode-labels local \
  --submit-mode-train local \
  --submit-mode-uq local
```

预期产物：

```bash
ls -l data/raw/geometry_pool_manifest.json results/pipeline_run_summary.json
```

## 2. 第一轮主线推荐跑法（PBS）

不要一开始就直接跑 `select_round_001`。  
第一轮正确顺序是：

1. `initial_selection -> build_delta_dataset`
2. `train_main_model -> evaluate_uncertainty`
3. 最后单独跑 `select_round_001`

### 2.1 先补齐第一轮数据集

```bash
python scripts/run_first_round_pipeline.py \
  --config configs/base.yaml \
  --from-stage initial_selection \
  --to-stage build_delta_dataset \
  --submit-mode-labels pbs
```

验收：

```bash
python - <<'PY'
import json
d = json.load(open("data/processed/delta_dataset_metadata.json"))
print("delta num_samples =", d.get("num_samples"))
PY
```

### 2.2 再跑训练与不确定性评估

```bash
python scripts/run_first_round_pipeline.py \
  --config configs/base.yaml \
  --from-stage train_main_model \
  --to-stage evaluate_uncertainty \
  --submit-mode-train pbs \
  --submit-mode-uq pbs
```

如果前面失败过，重跑时加 `--force`：

```bash
python scripts/run_first_round_pipeline.py \
  --config configs/base.yaml \
  --from-stage train_main_model \
  --to-stage evaluate_uncertainty \
  --submit-mode-train pbs \
  --submit-mode-uq pbs \
  --force
```

验收：

```bash
cat models/train_main_status.json
cat models/train_aux_status.json
cat results/uncertainty_status.json
ls -l models/training_state.json results/uncertainty_latest.json
```

### 2.3 最后再做下一轮选点

只有当 `results/uncertainty_latest.json` 已存在时，才能执行这一步：

```bash
python scripts/run_first_round_pipeline.py \
  --config configs/base.yaml \
  --from-stage select_round_001 \
  --to-stage select_round_001
```

验收：

```bash
ls -l results/round_001_selection_summary.json results/round_001_selected_manifest.json
```

## 3. 常见续跑方式

只重跑训练与 UQ：

```bash
python scripts/run_first_round_pipeline.py \
  --config configs/base.yaml \
  --from-stage train_main_model \
  --to-stage evaluate_uncertainty \
  --submit-mode-train pbs \
  --submit-mode-uq pbs \
  --force
```

只重跑 UQ：

```bash
python scripts/run_first_round_pipeline.py \
  --config configs/base.yaml \
  --from-stage evaluate_uncertainty \
  --to-stage evaluate_uncertainty \
  --submit-mode-uq pbs \
  --force
```

如果想直接本地验证 UQ：

```bash
python scripts/evaluate_uncertainty.py \
  --config configs/base.yaml \
  --manifest data/raw/geometry_pool_manifest.json \
  --submit-mode local \
  --output results/uncertainty_latest.json
```

## 4. 数据分析 notebook

- `docs/mace/数据分析.ipynb`
- 建议在产物生成后再打开，按单元顺序执行。
