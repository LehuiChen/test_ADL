# NequIP 结果说明（当前阶段）

对应工程：`minimal_adl_ethene_butadiene_nequip`

## 1. 结果目录对应

- 前置阶段产物：`minimal_adl_ethene_butadiene_nequip/data/`、`labels/`、`results/`
- 模型目录：`minimal_adl_ethene_butadiene_nequip/models/`（训练未实装阶段可能为空或不完整）

## 2. 当前可验收文件

- `results/check_environment_latest.json`
- `data/processed/delta_dataset.npz`
- `data/processed/delta_dataset_metadata.json`

## 3. 验收标准

- `check_environment --strict` 通过
- 前置阶段可跑通到 `build_delta_dataset`
- 触发训练时返回预期未实现提示，不出现误导性错误

## 4. 数据分析 notebook

- `docs/nequip/数据分析.ipynb`
- 当前主要用于前置阶段产物检查与结果汇总。
