# ANI 结果说明

对应工程：`minimal_adl_ethene_butadiene`

## 1. 结果目录

- 模型产物：`minimal_adl_ethene_butadiene/models/`
- 轮次结果：`minimal_adl_ethene_butadiene/results/`
- 累计数据：`minimal_adl_ethene_butadiene/data/processed/`

## 2. 关键文件

`data/processed/`

- `cumulative_labeled_manifest.json`
- `delta_dataset.npz`
- `delta_dataset_metadata.json`

`models/`

- `training_summary.json`
- `training_state.json`
- `train_main_predictions.csv`
- `train_aux_predictions.csv`
- `training_diagnostics.json`

`results/`

- `check_environment_latest.json`
- `ts_seed_summary.json`
- `round_000_initial_conditions_manifest.json`
- `round_001_md_frame_manifest.json`
- `round_001_md_trajectory_summary.json`
- `round_001_selection_summary.json`
- `round_001_selected_manifest.json`
- `active_learning_round_history.json`
- `pipeline_run_summary.json`

## 3. 结果怎么判定是对的

- `ts_seed_summary.json` 里应看到 `16` 个原子、`Charge=0`、`Multiplicity=1`、`1` 个虚频
- `cumulative_labeled_manifest.json` 的 `sample_id` 应与 label 目录完全一致
- `delta_dataset_metadata.json` 的 `num_samples` 应与累计 manifest 一致
- `round_001_md_frame_manifest.json` 应包含“每条轨迹首个不确定点”的 `sample_id / trajectory_id / frame_index / time_fs / uncertainty`
- `round_001_md_trajectory_summary.json` 里应能看到 `stopped_by_uncertainty`
- `round_001_selection_summary.json` 应包含 `selected_count`、`uncertain_ratio`、`converged`

## 4. 退役说明

旧的静态几何池主流程已经退役，当前验收与分析都只看 TS seed、累计标注数据、MD 返回点 manifest 和轮次选点结果。

## 5. 数据分析 notebook

- `docs/ani/数据分析.ipynb`
- notebook 现在围绕 TS seed、累计标注数据、MD 返回点 UQ 和轮次选点结果来分析
