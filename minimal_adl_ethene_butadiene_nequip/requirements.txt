# Base dependencies
numpy>=1.24
PyYAML>=6.0
mlatom>=3.18
matplotlib>=3.7
seaborn>=0.12
joblib>=1.3
scikit-learn>=1.3

# Model backend notes (install in your training env)
# torch is required for ANI/MACE/NequIP backends.
# NequIP backend placeholder: install `nequip` when implementing the adapter.
# Example (manual): pip install nequip
